<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.alphabeta.platform.base.dao.mapper.ext.XmManagerRoleExtMapper">
  <resultMap id="BaseResultMap" type="com.alphabeta.platform.base.domain.model.XmManagerRole">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="role_id" property="roleId" jdbcType="BIGINT"/>
    <id column="manager_id" property="managerId" jdbcType="BIGINT"/>
    <result column="state" property="state" jdbcType="CHAR"/>
    <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
    <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
  </resultMap>
  <sql id="Base_Column_List">
    role_id, manager_id, state, create_time, update_time
  </sql>
  <select id="checkRoleExist" resultType="java.lang.Boolean" parameterType="java.lang.Long">
    SELECT EXISTS (
    SELECT manager_id, role_id FROM xm_manager_role
    WHERE role_id=#{roleId}
    )
  </select>
  <select id="checkManagerRoleExist" resultType="java.lang.Boolean" parameterType="java.util.Map">
    SELECT EXISTS (
    SELECT manager_id, role_id FROM xm_manager_role
    WHERE manager_id=#{managerId} AND role_id=#{roleId}
    )
  </select>
  <select id="queryManagerRole" resultMap="BaseResultMap" parameterType="java.util.Map">
    SELECT
    <include refid="Base_Column_List"/>
    FROM xm_manager_role
    WHERE manager_id = #{managerId}
    <if test="state != null">
      AND state!=#{state}
    </if>
  </select>
</mapper>
